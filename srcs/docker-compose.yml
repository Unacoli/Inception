version: '3'

services:

  nginx:
    image: nginx:latest
    networks:
      - nginx
    ports:
      - 443:443
    volumes:
      - ./requirements/nginx/conf/:/etc/nginx/conf.d/:ro
      - ./nginx/tools:/etc/nginx/:ro
    restart: unless-stopped

networks:
  nginx:
