version: '3'

services:

  nginx:
    container_name: nginx
    image: nginx:latest
    networks:
      - nginx
    ports:
      - 443:443
    volumes:
      - ./requirements/nginx/conf/:/etc/nginx/conf.d/:ro
    restart: unless-stopped

networks:
  nginx:
